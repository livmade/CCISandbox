# CircleCI configuration file
version: 2.1

jobs:
  build:
    docker:
     - image: circleci/doc-highid
    steps:
        - run: echo "Hello, it me"

workflows:
  hello:
    jobs:
      - build