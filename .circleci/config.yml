# CircleCI configuration file
version: 2.1

jobs:
  testJob:
    docker:
    - image: salesforce/salesforcedx:latest-slim
    steps:
      - run: echo "Hello, from Salesforce!"


workflows:
  workflowtest:
    jobs:
     - testJob

### Comment to update build