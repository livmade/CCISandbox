# CircleCI configuration file
version: 2.1

jobs:
  testJob:
    docker:
    - image: cimg/base:2023.03
    steps:
      - run:
          name:  Print Workflow number?
          command: |
              echo "Workflow Number:  $CIRCLE_WORKFLOW_ID"






          


workflows:
  workflowtest:
    jobs:
     - testJob