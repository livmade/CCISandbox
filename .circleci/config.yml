# CircleCI configuration file
version: 2.1

jobs:
  testJob:
    docker:
    - image: salesforce/salesforcedx:nightly-full
    steps:
      - run: echo "Hello, from Salesforce!"


workflows:
  workflowtest:
    jobs:
     - testJob

### Comment to update build