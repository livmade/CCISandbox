version: 2.1

orbs:
  android: circleci/android@2.3.0

jobs:
  build:
    executor: 
        name: android/android-machine
        resource-class: large
        tag: 2023.07.1
    steps:
      - checkout
      - run: echo "Hello"

workflows:
  my-workflow:
    jobs:
      - build