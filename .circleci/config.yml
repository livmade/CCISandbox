version: 2.1

workflows:
  workflow-uno:
    jobs:
      - yjit-in-here

jobs:
  yjit-in-here:
    docker:
      - image: cimg/ruby:3.2.1

    steps:
      - checkout
      - run:
          name: Yjit Installed?
          command: |
            ruby -v --yjit